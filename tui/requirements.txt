# Optional full-screen TUI (stdlib REPL works without these)
textual>=0.80.0
rich>=13.0.0
# Optional in-pane stills / thumbs (fallbacks: chafa CLI, path-only)
rich-pixels>=3.0.0
pillow>=10.0.0
# Optional: textual-image for Kitty/Sixel protocols when terminal supports them
# textual-image>=0.8.0
