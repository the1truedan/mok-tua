# Repeated lab-context bundle

These are the compact procedural facts worth recalling before any cloud or local agent prompt. They are intentionally operational, not a transcript.

1. **Storage:** NFS `ai-data` is for models, artifacts, repositories, and backups. Keep live SQLite/databases, locks, journals, and high-churn indexes on local disk. Use bounded, allowlisted walks; never scan all of `/ai-data` by default.
2. **Network:** use the documented SSH host/user and strict bounded commands with connect timeouts. Tower (`control-host`) and gpu-host (`gpu-host`) are single-command-at-a-time operations; do not probe repeatedly or run unbounded remote file walks.
3. **Gateway:** Headroom is the context-budget/compression layer before LiteLLM. Turnstone owns gpu-host workstreams; pi/omp are local executors; Herdr is a supervised TUI, not a competing scheduler. Cloud fallback requires a handoff and independent local review.
4. **Paths:** `/ai-data`/`/mnt/ai-data` is the canonical shared model/artifact tree. Pinokio and Stability Matrix configs must not retain stale `/Volumes/2TB` model/cache paths. UV caches are host-split and use copy link mode on NFS.
5. **Permissions:** repair ownership/mode for the exact subtree, then run a bounded read/write/execute probe as the lab operator account; do not chmod the entire share recursively. Record NFS-version differences and the exact path in an incident note.
6. **History/memory:** AgentsView is session history, Hippo is durable approved context, and raw PasteBar/clipboard/chat exports are not automatic memory input. Promote only reviewed, redacted reminders tagged `agent-context` or `repeated-reminder`.
7. **Documentation:** `~/grokcode/docs` is the operational source of truth. Every incident or cloud-credit handoff records objective, host, paths, commands, changed files, tests, unresolved risks, and next action.
8. **Big coding jobs:** send via tok-tua → Headroom → LiteLLM (prefer `manager-code` / gpu-host). Preload only Hippo tags `agent-context` / `repeated-reminder`. On context-full or length-stop: **compact or new session with citation packet** (AgentsView session URL, Hippo mem ids, `docs/*` paths, latest `SESSION_HANDOFF_*`) — not a full-history dump to desk-host spillover. Session sources to *cite*, not bulk-replay: `~/.pi/agent/sessions`, `~/.codex/sessions`, `~/.local/share/opencode/opencode.db`, AgentsView; code-index is symbol wiki only. See `~/grokcode/docs/operations/SESSION_HANDOFF_2026-08-04_JOHNNY_PUBLIC_AND_CONTEXT_ROUTING.md`.
9. **Plan cloud · execute local (Claude lockout lesson):** Claude Desktop/Cowork is **not** behind Headroom — do not use it for multi-hour bash/ClickUp/screenshot loops. Forensic: `cowork:c51b5f70-…` (~983k peak ctx, ~430k output, 1 compact) — `~/grokcode/docs/operations/TOKEN_BURN_FORENSIC_CLAUDE_LMSTUDIO_2026-08-02.md`. Cloud ≤15 turns plan-only → write `HANDOFF_*.md` + PLAN → new local `role-execute`/`manager-code` via Headroom. Hard stop cloud at ≥55–75% context; never continue the burn thread.
10. **Context pit + elevated release (2026-08-05):** All brands start at `~/grokcode/docs/operations/AGENT_CONTEXT_PIT_AND_MULTI_CLI_CONTINUITY_2026-08-05.md` before filewalk/SSH. AgentsView `http://127.0.0.1:42030/` is an **ID index**, not a dump. **mok-tua Pinokio orchestration** is elevated in public-flip rank ahead of Johnny — plan: `~/grokcode/docs/roadmap/MOK_TUA_PINOKIO_ORCHESTRATION_RELEASE_PLAN_2026-08-05.md`. Private A.I.D.A. proof lab: `~/aida-complex-doc-lab` (no PHI bodies). Forgejo remotes must not embed userinfo (scrubbed).
11. **I2V provenance (Grok ≠ gpu-host):** Hybrid MANAGER vibe mp4 used **Grok Imagine I2V + ffmpeg** (no gpu-host CUDA). Canonical **local** proof is Comfy stills + **AnimateDiff** on gpu-host (`docs/reports/gpu-host_LOCAL_RENDER_SMOKE_2026-08-05.md`). Incident: `docs/operations/I2V_GROK_VS_gpu-host_CONTEXT_AMBIGUITY_INCIDENT_2026-08-05.md`. Every clip needs `renderer` + `qqq` + `gpu_evidence`.
12. **FramePack shared models:** launch only via `scripts/run_framepack_shared_models.sh`; `hf_download` → `/mnt/ai-data/models/hf_hub`; host venv under `~/pinokio-host-runtimes/framepack-linux-amd64`; `--install-deps` for uv batch; prefer `--offline`. NFS uid 501 vs 1000 → write fallback to `$HOME/work/framepack`.
13. **Full gamut + C64:** framework `docs/roadmap/FULL_GAMUT_gpu-host_FRAMEWORK_2026-08-05.md`; handoff `docs/operations/SESSION_HANDOFF_2026-08-05_FULL_GAMUT_STAGED_SMOKE.md`; C64 deck `python3 scripts/mok_tua_cli.py tui --repl --skin c64`; unit tests 12 OK; public flip human-only after protect runbook.
14. **wait-what (public GH English):** earmark [mattpocock/skills](https://github.com/mattpocock/skills) skill `wait-what` before README/Release/PR posts — plain re-pitch (ASD-STE100-style) for native English speakers, **not** AI aficionados. Lab ops docs keep jargon. Doc: `docs/roadmap/WAIT_WHAT_GITHUB_PLAIN_ENGLISH_EARMARK_2026-08-05.md`.
